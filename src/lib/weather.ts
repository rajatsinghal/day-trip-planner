// Re-exported from @dtp/core — shared with mobile.
export type {
  DailyWeather,
  HourlyWeather,
  WeatherResponse,
} from '@dtp/core';
export {
  aggregateHourlyToDaily,
  scoreWeather,
  weatherCodeToLabel,
} from '@dtp/core';
